1、功能说明

	这个例程配置并演示PMP进行隐私保护，防止用户模式应用访问敏感数据内容
        用户代码运行在用户模式下，而隐私保护仅允许在机器模式下访问；

	样例中privacy.c中实现了一组隐私加密保护的代码，其中加密算法及密钥为敏感内容，通过PMP配置该段空间在用户模式下不可访问；
	样例中privacy_api.c中实现了隐私接口访问的API，该接口通过系统调用ECALL进入异常模式实现模式切换并进行隐私接口访问；
	样例中实现了两种调用隐私接口的方式，
		1.首先系统调用API进行接口访问，该方式将不受PMP配置约束可正常执行；
		2.直接调用隐私内部API进行接口访问，由于当前处于用户模式下，该空间被PMP所保护，数据访问将不被允许

2、使用环境

	/* 软件开发环境：当前工程使用的软件工具名称及版本号 */
    	IDE工具：NucleiStudio IDE for C/C++ 2021-02
	
	/* 开发板 */
	CM32M4xxR_LQFP128_STB

3、使用说明

	/* 描述相关模块配置方法；例如:时钟，I/O等 */
        1、SystemClock：144MHz
        2、USART配置如下：
                - 波特率 = 115200 baud
                - 字长 = 8数据位
                - 1停止位
                - 校验控制禁用
                - 硬件流控制禁用（RTS和CTS信号）
                - 接收器和发送器使能
        3、通过USB Serial连接开发板或外置链接USART引脚连接如下：
                - UART5_Tx.PE8
                - UART5_Rx.PE9	
	
	使用方法：
		1.编译后下载程序复位运行；
	    	2.程序运行后，将在log串口打印分别通过系统调用API访问隐私接口和直接访问隐私接口的日志信息，在直接访问时将打印PMP异常信息
		
4、注意事项
	PMP的最小slot单位为128bytes

	